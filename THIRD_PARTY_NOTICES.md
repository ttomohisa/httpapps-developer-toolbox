# Third-Party Notices

Developer Toolbox 1.0.0 contains no bundled third-party runtime library code.

The application uses browser APIs and system fonts directly. The repository build/deployment workflows reference GitHub-maintained Actions under the terms published by those projects.

If a future version adds a package to `dependencies.json`, pin the exact version and record its license, homepage, required notices, and every embedded runtime asset here before release.
